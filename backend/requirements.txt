# AgriSathi Backend — Windows Local Requirements
# GPU/Training packages (only needed in Colab for fine-tuning)
# Install these only in Google Colab, NOT locally

# ── Core API ──────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
python-multipart==0.0.9

# ── Data & Evaluation ─────────────────────────────────
pandas>=2.0.0
scikit-learn>=1.4.0
rouge-score>=0.1.2
nltk>=3.8.1

# ── RAG (CPU mode - works locally) ───────────────────
langchain>=0.2.0
langchain-community>=0.2.0
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0

# ── Optional (Colab only - GPU required) ──────────────
# torch>=2.1.0
# transformers>=4.40.0
# peft>=0.10.0
# trl>=0.8.6
# bitsandbytes>=0.43.0
# unsloth[colab-new]
